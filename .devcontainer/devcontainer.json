{
    "name": "SpeakPal dev",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "features": {
        "ghcr.io/devcontainers/features/node:1": {
            "version": "22"
        }
    },
    "remoteUser": "dev",
    "workspaceFolder": "/workspaces/SpeakPal",
    "forwardPorts": [3000, 8000],
    "containerEnv": {
        "SHELL": "/bin/zsh"
    },
    "postCreateCommand": "bash -lc 'sudo pip install -r backend/requirements-dev.txt && npm --prefix frontend install && npm install -g @anthropic-ai/claude-code'"
}
